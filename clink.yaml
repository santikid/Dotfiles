ignore:
  - .git # needed because we have a submodule
  - .DS_Store

features:
  - slug: all
    target: ~/
    enabled: all

  - slug: mac
    target: ~/
    enabled: macos

  - slug: linux
    target: ~/
    enabled: linux

  - slug: server
    target: ~/
    enabled: none
